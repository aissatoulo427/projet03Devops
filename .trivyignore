# CVEs ignorées temporairement car les versions corrigées ne sont pas encore
# disponibles dans le dépôt Alpine utilisé par l'image de base nginx.
# À retirer dès que alpine publie les packages corrigés.

# CVE-2026-28390 : openssl (libcrypto3/libssl3) - fix attendu : 3.5.6-r0
CVE-2026-28390

# CVE-2026-22184 : zlib - fix attendu : 1.3.2-r0
CVE-2026-22184
